# Stripe (required for card payments)
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_xxx
STRIPE_SECRET_KEY=sk_live_xxx

# Stripe webhook signing secret (for verifying webhook events from Stripe Dashboard)
# STRIPE_WEBHOOK_SECRET=whsec_xxx

# Discord webhook for order notifications (Stripe/PayPal)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/xxx/xxx

# Resend (order emails + newsletter sender tool)
RESEND_API_KEY=re_xxx
RESEND_FROM=Vasaros Kampelis <v@vasaroskampelis.com>

# Newsletter: dedup keys nl:email:*, subscriber list nl:subscribers_txt (npm run export:newsletter-txt)
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
